# 当前脚手架默认使用的网络别名，需要和 hardhat.config.js 保持一致。
NETWORK_NAME=gsc_v2_test
# 目标链 RPC 地址。
RPC_URL=https://your-rpc.example
# 目标链 chainId。
CHAIN_ID=44
# 仅用于测试的钱包私钥，不要使用生产钱包。
PRIVATE_KEY=0xyourprivatekey
# 合约名会影响 ABI 文件名、测试文件名和文档名。
CONTRACT_NAME=Contract
# 已部署合约地址；如果暂时没有，读/写测试会保持受限或跳过。
CONTRACT_ADDRESS=
